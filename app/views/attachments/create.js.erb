
<% if @attachment.new_record? %>
  alert("Failed to upload attachment: <%= j @attachment.errors.full_messages.join(', ') %>");
<% else %>
  $("#attachments").prepend("<%= j render @attachment %>");
  intialize_popovers();
<% end %>

